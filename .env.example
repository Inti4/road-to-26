# No API keys are required to run Road to 26.
# Live data comes from the free, public WorldCup26.ir feed (no key, no auth).
#
# Match times default to Eastern (America/New_York); switch zones in the UI.
